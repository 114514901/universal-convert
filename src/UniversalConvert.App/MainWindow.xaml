<Window x:Class="UniversalConvert.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UniversalConvert" Height="520" Width="560"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True" Drop="OnDrop" DragOver="OnDragOver">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="20" FontWeight="SemiBold"
                   Text="UniversalConvert" Margin="0,0,0,4"/>

        <Border Grid.Row="1" BorderBrush="#BBBBBB" BorderThickness="1" CornerRadius="6"
                Background="#F7F7F7" Padding="16">
            <StackPanel>
                <TextBlock Text="将文件拖到此处，或点击选择" Foreground="#666666" HorizontalAlignment="Center"/>
                <TextBlock x:Name="SelectedFileText" Text="未选择文件" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,8,0,0" TextWrapping="Wrap"/>
                <Button Content="选择文件..." HorizontalAlignment="Center" Margin="0,12,0,0"
                        Padding="16,6" Click="OnSelectFile"/>
            </StackPanel>
        </Border>

        <GroupBox Grid.Row="2" Header="可转换的目标格式" Margin="0,12,0,0">
            <ListBox x:Name="OutputList" SelectionMode="Single"
                     HorizontalContentAlignment="Stretch"
                     SelectionChanged="OnOutputSelectionChanged"/>
        </GroupBox>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CustomizeButton" Content="自定义..." Height="36" FontSize="14"
                        Width="110" Margin="0,0,8,0" IsEnabled="False" Click="OnCustomize"/>
                <Button x:Name="ConvertButton" Content="转换" Height="36" FontSize="14"
                        Width="120" IsEnabled="False" Click="OnConvert"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
